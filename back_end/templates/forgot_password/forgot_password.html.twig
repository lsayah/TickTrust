<!DOCTYPE html>
<html lang="fr">

<head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
    <meta name="description" content="TickTrust - Réinitialisation de mot de passe">
    <meta name="author" content="TickTrust">

    <title>Réinitialisation de Mot de Passe</title>

    <link href="{{ asset('vendor/fontawesome-free/css/all.min.css') }}" rel="stylesheet" type="text/css">
    <link href="https://fonts.googleapis.com/css?family=Nunito:200,200i,300,300i,400,400i,600,600i,700,700i,800,800i,900,900i" rel="stylesheet">
    <link href="{{ asset('css/sb-admin-2.min.css') }}" rel="stylesheet">

    <style>
        /* Centrer verticalement et horizontalement */
        html, body {
            background: #f8f9fc;
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100%;
            margin: 0;
        }

        /* Card Styling */
        .card {
            width: 100%;
            max-width: 450px;
            border-radius: 10px;
            box-shadow: 0 1px 15px rgba(0, 0, 0, 0.1);
            overflow: hidden;
        }

        .card-body {
            padding: 3rem;
        }

        .form-group {
            margin-bottom: 1.5rem;
        }

        /* Input field styling */
        .form-control {
            border-radius: 30px;
            padding: 20px;
            font-size: 1rem;
            border: 1px solid #ddd;
        }

        .btn-primary {
            width: 100%;
            border-radius: 30px;
            padding: 15px;
            font-size: 1.1rem;
            background-color: #4e73df;
            border-color: #4e73df;
            transition: all 0.3s ease-in-out;
        }

        .btn-primary:hover {
            background-color: #2e59d9;
            border-color: #2653d4;
        }

        .text-center a {
            font-size: 0.9rem;
            color: #4e73df;
            transition: all 0.3s ease-in-out;
        }

        .text-center a:hover {
            text-decoration: underline;
            color: #2e59d9;
        }

        .text-center h1 {
            font-size: 1.8rem;
            font-weight: 700;
            margin-bottom: 1rem;
            color: #333;
        }

        .text-center p {
            font-size: 1.1rem;
            color: #6c757d;
        }
    </style>
</head>

<body>

    <div class="container">
        <div class="card o-hidden border-0 shadow-lg my-5">
            <div class="card-body">
                <div class="row">
                    <div class="col-lg-12 d-flex align-items-center justify-content-center">
                        <div class="p-5 w-100">
                            <div class="text-center">
                                <h1 class="h4 text-gray-900 mb-2">Mot de Passe Oublié ?</h1>
                                <p class="mb-4">Pas de panique, entrez votre adresse email et nous vous enverrons un lien pour réinitialiser votre mot de passe.</p>
                            </div>

                            <!-- Start Form -->
                            {{ form_start(forgotPasswordForm, {'attr': {'class': 'user'}}) }}
                                <div class="form-group">
                                    {{ form_row(forgotPasswordForm.email, {
                                        'attr': {
                                            'class': 'form-control form-control-user',
                                            'placeholder': 'Entrez votre adresse email'
                                        }
                                    }) }}
                                </div>
                            {{ form_end(forgotPasswordForm) }}
                            <!-- End Form -->

                            <div class="text-center mt-3">
                                <a class="small" href="{{ path('app_registration') }}">Créer un compte</a>
                            </div>
                            <div class="text-center mt-2">
                                <a class="small" href="{{ path('app_login') }}">Vous avez déjà un compte ? Connectez-vous</a>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>

    <!-- Bootstrap JavaScript-->
    <script src="{{ asset('vendor/jquery/jquery.min.js') }}"></script>
    <script src="{{ asset('vendor/bootstrap/js/bootstrap.bundle.min.js') }}"></script>

    <!-- Custom Scripts-->
    <script src="{{ asset('vendor/jquery-easing/jquery.easing.min.js') }}"></script>
    <script src="{{ asset('js/sb-admin-2.min.js') }}"></script>

</body>

</html>

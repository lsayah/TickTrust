{% extends 'admin/dashboard_admin.html.twig' %}

{% block title %}Profil{% endblock %}

{% block body %}
<div class="container-fluid">
    <h1 class="h3 mb-4 text-gray-800">Profil</h1>
    <div class="card shadow mb-4">
        <div class="card-header py-3">
            <h6 class="m-0 font-weight-bold text-primary">Informations du profil</h6>
        </div>
        <div class="card-body">
            <p><strong>Nom:</strong> {{ user.nom }}</p>
            <p><strong>Prénom:</strong> {{ user.prenom }}</p>
            <p><strong>Email:</strong> {{ user.email }}</p>
            <a href="{{ path('app_profile_edit') }}" class="btn btn-primary">Modifier le profil</a>
            <a href="{{ path('app_profile_change_password') }}" class="btn btn-warning">Changer le mot de passe</a>
        </div>
    </div>
</div>
{% endblock %}